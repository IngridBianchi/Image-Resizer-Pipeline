# Copia este archivo como .env y completa los valores reales.
# NUNCA subas .env con credenciales al repositorio.

AWS_REGION=us-east-1
INPUT_BUCKET=image-resizer-input-5c795f19
OUTPUT_BUCKET=image-resizer-output-5c795f19

# Origen permitido para CORS (frontend dev server)
CORS_ORIGIN=http://localhost:5173

# Puerto del servidor (opcional, default 3001)
PORT=3001
